{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma db push && next build",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
